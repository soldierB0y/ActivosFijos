@model IEnumerable<ActivosFijos.Models.TiposActivo>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Descripicion)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.CuentaContableCompra)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.CuentaContableDepreciacion)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Estado)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Descripicion)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.CuentaContableCompra)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.CuentaContableDepreciacion)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Estado)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.IdtiposActivos">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.IdtiposActivos">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.IdtiposActivos">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
